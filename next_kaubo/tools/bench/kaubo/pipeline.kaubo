// Pipeline 100k — 条件分支 + 算术
var pipeline = || -> int {
    var total = 0; var x = 1;
    while x <= 100000 {
        if x % 2 != 0 { var t = x * 3; if t % 7 == 0 { total = total + t; } }
        x = x + 1;
    }
    return total;
};
print(pipeline());
