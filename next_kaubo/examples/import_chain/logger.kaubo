// Logger module - base of the import chain
// Provides basic logging functionality

pub var log_level = "INFO";

pub var log = |message| {
    print("[" + log_level + "] " + message);
};

pub var debug = |message| {
    if log_level == "DEBUG" {
        print("[DEBUG] " + message);
    }
};
